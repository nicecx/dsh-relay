# 安全模型（Security Model）

dsh-relay 把"远程通道 = 绕过本机批准体系"当作首要威胁。以下红线不可妥协，
任何改动不得放宽；对应回归测试见 [TESTING.md](TESTING.md) 的"历史修复锚点"。

## 威胁模型

- 攻击者向通道发送消息，试图让插件执行危险操作（批准/拒绝/回答/开关），
  或诱导 agent 读取/外发本机敏感信息；
- 攻击者伪造发件人（iMessage 句柄 / 邮件地址 / 微信 user id）；
- 同一通道上存在其他机器人（如 ops-agent），消息互相可见。

## 红线

1. **发送方白名单**：每个通道只应答配置的白名单身份
   （iMessage `handle`/`extraHandles`、Email `from`/`to`/`allowedFrom`、微信扫码白名单）；
   非白名单消息一律忽略并告警。
2. **通道命令是固定白名单**：只有 批准/拒绝/回答/回复/开关/会话配置/列表 等命令，
   **没有任何"执行 shell / 读文件 / 改配置"类命令**。
3. **编号必须对应真实 pending**：应答只认本插件分配且未过期的编号；
   超时/撤销/重启后失效（stale 不可应答）；外来编号静默。
4. **注入护栏**：通道文本只能注入会话流（`source.kind='plugin'`），注入时附带安全提示
   （不得外发密钥/令牌/凭据、不得改仓库配置、高危操作照常走审批）；
   注入目标**只进显式绑定会话**（`/bind` 或 `boundSessions`），禁止跟随"最近活跃"。
5. **外发脱敏**：推送片段自动抹除常见密钥/令牌/私钥/凭据赋值（`security.redactSecrets`）。
6. **邮件发件人认证**：解析 `Authentication-Results`（SPF/DKIM/DMARC），任一 `fail` 拒绝；
   `email.strictAuth: true` 时无认证头也拒绝。
7. **iMessage 自回灌防护**：只收 `is_from_me=0` 或带可信句柄的消息，
   用不可见自标记 `D5HR42` 排除插件自身推送。
8. **总开关可随时关闭**：通道内 `关闭` 立即停用全部推送/应答，状态持久化。

## 配置建议

- 个人配置（句柄/邮箱/账号）只放机器本地（profile 的 `cordis.patch.yml`），**不进仓库**；
- Email 凭据走 `~/.dsh/.credentials.yaml`（`passRef`），不落明文；
- 微信通道默认禁用（个人微信自动化有风控风险）。

## 报告漏洞

发现漏洞请通过仓库 Issues 私密提交或联系维护者，请勿公开披露未修复的安全问题。
